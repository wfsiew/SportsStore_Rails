<%= link_to 'Home', product_path %>
<%= link_to t('cart.shoppingcart'), cart_path %>
<% @categories.each do |c| %>
  <% opt = {} %>
  <% opt[:class] = 'selected' if c.category == @category %>
  <%= link_to c.category, category_path(:category => c.category), opt %>
<% end %>
